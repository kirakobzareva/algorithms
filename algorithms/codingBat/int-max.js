// Given three int values, a b c, return the largest.
//
//
// intMax(1, 2, 3) → 3
// intMax(1, 3, 2) → 3
// intMax(3, 2, 1) → 3

function intMax(a, b, c) {
    console.log(Math.max(a,b,c));
}

intMax(-3,-2,-1);