// import {mixStart} from "../mix-start.js";
//
//
// describe("mixStart", () => {
//     beforeEach(function () {
//     });
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//     "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("mix snacks");
//         expect(result).toEqual(true);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("pix snacks");
//         expect(result).toEqual(true);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("piz snacks");
//         expect(result).toEqual(false);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("nix");
//         expect(result).toEqual(true);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("ni");
//         expect(result).toEqual(false);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("n");
//         expect(result).toEqual(false);
//     })
//     it("should return true true if the given string begins with 'mix', except the 'm' can be anything " +
//         "so 'pix', '9ix' .. all count", function () {
//         const result = mixStart("n");
//         expect(result).toEqual(false);
//     })
//
// });